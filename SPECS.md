# Project Specification

*This file is empty until the first evolution session.*

When the evolution agent first runs, it reads project requirements from:
1. The bead description (provided by the Mayor when dispatching work)
2. GitHub issues tagged with the configured label
3. Direct user input

The agent writes the specification here, then builds ROADMAP.md from it.

---

<!-- REPLACE EVERYTHING ABOVE WITH YOUR PROJECT SPECIFICATION -->
